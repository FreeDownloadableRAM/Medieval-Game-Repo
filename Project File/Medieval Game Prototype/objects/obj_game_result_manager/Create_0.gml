// what game mode are we in?
// this is passed through from other objects in menus prior to a match starting
// in campaign this would be lvl select, or vs ai mode.
current_gamemode = global.game_mode;

// game win conditions are handled by other objects, like the king or camp, this just spawns in related objects
// once conditions are fulfilled
